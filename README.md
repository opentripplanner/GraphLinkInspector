# OTP GraphLinkInspector

Utility script for inspecting a built OTP graph to detect and list instances of a transit stop being linked to a specific edge type (currently streets with "highway=service" tag).
